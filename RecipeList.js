/*import React, { Component } from "react";

class RecipeList extends Component {
  constructor(props) {
    super(props);
    //console.log(this.state)
  }

  render() {
    const recipyState = this.props.entriesRecipy;
    var names = recipyState.map((item, i) => <li>{item.name}</li>);
    console.log(this.state);
    return (
      <div>
        <ul>{names}</ul>
      </div>
    );
  }
}

export default RecipeList;*/
